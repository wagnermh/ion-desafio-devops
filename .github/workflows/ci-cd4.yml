name: Build and Deploy - v 4.0

#on:
#  push:
#    branches: [ main ]
  workflow_dispatch:

env:
  IMAGE_NAME: ion-desafio-devops-app
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
  IMAGE_TAG: 1.${{ github.run_number }}
  IMAGE_FULL: ${{ secrets.DOCKER_USERNAME }}/ion-desafio-devops-app:1.${{ github.run_number }}
  IMAGE_TAR: /tmp/image.tar

jobs:
  # ================================================================
  # JOB 1: CHECKOUT DO CÓDIGO
  # ================================================================
  checkout-code:
    runs-on: ubuntu-latest
    outputs:
      code_checked: ${{ steps.checkout.outcome }}
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7.0.0
        id: checkout

      - name: 📂 List repository files
        run: |
          echo "📂 Repository contents:"
          ls -la
          echo "✅ Code checked out successfully"

  # ================================================================
  # JOB 2: BUILD DA IMAGEM (LOCALMENTE + SAVE TAR)
  # ================================================================
  build-image:
    needs: checkout-code
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ env.IMAGE_TAG }}
      image_exists: ${{ steps.check-image.outputs.exists }}
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7.0.0

      - name: 🏗️ Build Docker image locally
        run: |
          echo "📦 Building image: ${{ env.IMAGE_FULL }}"
          docker build -t ${{ env.IMAGE_FULL }} -t ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest .
          echo "✅ Image built with tags:"
          docker images --format "{{.Repository}}:{{.Tag}}" | grep "${{ env.IMAGE_NAME }}"

      - name: 💾 Save image to tar file
        run: |
          echo "💾 Saving image to ${{ env.IMAGE_TAR }}"
          docker save ${{ env.IMAGE_FULL }} ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest -o ${{ env.IMAGE_TAR }}
          ls -lh ${{ env.IMAGE_TAR }}

      - name: 📤 Upload image tar as artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: docker-image-tar
          path: ${{ env.IMAGE_TAR }}
          retention-days: 1

      - name: 🔍 Verify image exists locally
        id: check-image
        run: |
          echo "🔍 Checking if images exist locally..."
          if docker images --format "{{.Repository}}:{{.Tag}}" | grep -q "${{ env.IMAGE_FULL }}"; then
            echo "✅ Image found: ${{ env.IMAGE_FULL }}"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Image not found locally!"
            echo "exists=false" >> $GITHUB_OUTPUT
            exit 1
          fi
          
          if docker images --format "{{.Repository}}:{{.Tag}}" | grep -q "${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest"; then
            echo "✅ Image found: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest"
          else
            echo "❌ Warning: latest tag not found locally!"
          fi
          
          echo "📦 Available images:"
          docker images --format "table {{.Repository}}:{{.Tag}}\t{{.Size}}" | grep "${{ env.IMAGE_NAME }}"

      - name: 📊 Generate build report
        run: |
          echo "## 🏗️ Build Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Information | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|-------------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Image Name** | ${{ env.IMAGE_NAME }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Tag** | ${{ env.IMAGE_TAG }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Full Image** | ${{ env.IMAGE_FULL }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Latest Tag** | ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest |" >> $GITHUB_STEP_SUMMARY
          echo "| **Status** | ✅ Built locally |" >> $GITHUB_STEP_SUMMARY

  # ================================================================
  # JOB 3: SCANS DE SEGURANÇA (EXECUTADOS EM PARALELO)
  # ================================================================
  
  # ---------- SCAN 1: TRIVY ----------
  trivy-scan:
    needs: build-image
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check.outputs.status }}
      critical: ${{ steps.check.outputs.critical }}
      high: ${{ steps.check.outputs.high }}
      medium: ${{ steps.check.outputs.medium }}
      sarif_path: trivy-results.sarif
    steps:
      - name: 📥 Download image tar
        uses: actions/download-artifact@v8
        with:
          name: docker-image-tar
          path: /tmp

      - name: 📦 Load image from tar
        run: |
          echo "📦 Loading image from ${{ env.IMAGE_TAR }}"
          docker load -i ${{ env.IMAGE_TAR }}
          docker images

      - name: 🔒 Scan with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.IMAGE_FULL }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'

      - name: 📊 Generate Trivy report
        run: |
          echo "## 🔒 Trivy Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          if [ -f "trivy-results.sarif" ]; then
            CRITICAL=$(grep -o '"severity":"critical"' trivy-results.sarif | wc -l || echo "0")
            HIGH=$(grep -o '"severity":"high"' trivy-results.sarif | wc -l || echo "0")
            MEDIUM=$(grep -o '"severity":"medium"' trivy-results.sarif | wc -l || echo "0")
            LOW=$(grep -o '"severity":"low"' trivy-results.sarif | wc -l || echo "0")
            echo "| 🔴 **Critical** | $CRITICAL |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟠 **High** | $HIGH |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 **Medium** | $MEDIUM |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟢 **Low** | $LOW |" >> $GITHUB_STEP_SUMMARY
            echo "**Total:** $((CRITICAL + HIGH + MEDIUM + LOW))" >> $GITHUB_STEP_SUMMARY

            # --- Lista resumida de vulnerabilidades (IDs) ---
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📌 Vulnerabilidades encontradas (amostra)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Extrai ruleId (CVE) para cada severidade
            for SEV in critical high medium; do
              COUNT=$(grep -o "\"severity\":\"$SEV\"" trivy-results.sarif | wc -l)
              if [ "$COUNT" -gt 0 ]; then
                # Pega os primeiros 10 IDs
                IDS=$(grep -B2 "\"severity\":\"$SEV\"" trivy-results.sarif | grep -o '"ruleId":"[^"]*"' | sed 's/"ruleId":"//;s/"//' | head -10 | paste -sd ", " -)
                echo "**${SEV^}** ($COUNT): $IDS" >> $GITHUB_STEP_SUMMARY
                if [ "$COUNT" -gt 10 ]; then
                  echo "  _(+ $((COUNT - 10)) outros)_" >> $GITHUB_STEP_SUMMARY
                fi
                echo "" >> $GITHUB_STEP_SUMMARY
              fi
            done
          fi

      - name: 📤 Upload Trivy results
        uses: actions/upload-artifact@v7.0.1
        with:
          name: trivy-scan-results
          path: trivy-results.sarif
          retention-days: 7

      - name: ✅ Check scan status
        id: check
        run: |
          if [ ! -f "trivy-results.sarif" ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "critical=0" >> $GITHUB_OUTPUT
            echo "high=0" >> $GITHUB_OUTPUT
            echo "medium=0" >> $GITHUB_OUTPUT
            echo "❌ Scan failed - no results file"
            exit 0
          fi
          CRITICAL=$(grep -o '"severity":"critical"' trivy-results.sarif | wc -l || echo "0")
          HIGH=$(grep -o '"severity":"high"' trivy-results.sarif | wc -l || echo "0")
          MEDIUM=$(grep -o '"severity":"medium"' trivy-results.sarif | wc -l || echo "0")
          echo "critical=$CRITICAL" >> $GITHUB_OUTPUT
          echo "high=$HIGH" >> $GITHUB_OUTPUT
          echo "medium=$MEDIUM" >> $GITHUB_OUTPUT
          
          # 🔥 BARRAR APENAS: CRITICAL e HIGH (MEDIUM NÃO BLOQUEIA)
          if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 0 ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ Found $CRITICAL CRITICAL and $HIGH HIGH vulnerabilities!"
          else
            echo "status=passed" >> $GITHUB_OUTPUT
            echo "✅ No CRITICAL or HIGH vulnerabilities found"
          fi

  # ---------- SCAN 2: GRYPE ----------
  grype-scan:
    needs: build-image
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check.outputs.status }}
      critical: ${{ steps.check.outputs.critical }}
      high: ${{ steps.check.outputs.high }}
      medium: ${{ steps.check.outputs.medium }}
    steps:
      - name: 📥 Download image tar
        uses: actions/download-artifact@v8
        with:
          name: docker-image-tar
          path: /tmp

      - name: 📦 Load image from tar
        run: |
          echo "📦 Loading image from ${{ env.IMAGE_TAR }}"
          docker load -i ${{ env.IMAGE_TAR }}
          docker images

      - name: 📦 Install Grype
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
          grype version

      - name: 🔐 Scan with Grype
        run: |
          grype ${{ env.IMAGE_FULL }} -o json > grype-results.json
          grype ${{ env.IMAGE_FULL }} -o table > grype-results.txt

      - name: 📊 Generate Grype report
        run: |
          echo "## 🔐 Grype Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          if [ -f "grype-results.json" ]; then
            CRITICAL=$(grep -o '"severity":"Critical"' grype-results.json | wc -l || echo "0")
            HIGH=$(grep -o '"severity":"High"' grype-results.json | wc -l || echo "0")
            MEDIUM=$(grep -o '"severity":"Medium"' grype-results.json | wc -l || echo "0")
            LOW=$(grep -o '"severity":"Low"' grype-results.json | wc -l || echo "0")
            echo "| 🔴 **Critical** | $CRITICAL |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟠 **High** | $HIGH |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 **Medium** | $MEDIUM |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟢 **Low** | $LOW |" >> $GITHUB_STEP_SUMMARY
            echo "**Total:** $((CRITICAL + HIGH + MEDIUM + LOW))" >> $GITHUB_STEP_SUMMARY

            # --- Lista resumida de vulnerabilidades (IDs) ---
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📌 Vulnerabilidades encontradas (amostra)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Extrai vulnerability.id para cada severidade
            for SEV in Critical High Medium; do
              COUNT=$(grep -o "\"severity\":\"$SEV\"" grype-results.json | wc -l)
              if [ "$COUNT" -gt 0 ]; then
                # Pega os primeiros 10 IDs
                IDS=$(grep -B5 "\"severity\":\"$SEV\"" grype-results.json | grep -o '"id":"[^"]*"' | sed 's/"id":"//;s/"//' | head -10 | paste -sd ", " -)
                echo "**${SEV}** ($COUNT): $IDS" >> $GITHUB_STEP_SUMMARY
                if [ "$COUNT" -gt 10 ]; then
                  echo "  _(+ $((COUNT - 10)) outros)_" >> $GITHUB_STEP_SUMMARY
                fi
                echo "" >> $GITHUB_STEP_SUMMARY
              fi
            done
          fi

      - name: 📤 Upload Grype results
        uses: actions/upload-artifact@v7.0.1
        with:
          name: grype-scan-results
          path: |
            grype-results.json
            grype-results.txt
          retention-days: 7

      - name: ✅ Check scan status
        id: check
        run: |
          if [ ! -f "grype-results.json" ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "critical=0" >> $GITHUB_OUTPUT
            echo "high=0" >> $GITHUB_OUTPUT
            echo "medium=0" >> $GITHUB_OUTPUT
            echo "❌ Scan failed - no results file"
            exit 0
          fi
          CRITICAL=$(grep -o '"severity":"Critical"' grype-results.json | wc -l || echo "0")
          HIGH=$(grep -o '"severity":"High"' grype-results.json | wc -l || echo "0")
          MEDIUM=$(grep -o '"severity":"Medium"' grype-results.json | wc -l || echo "0")
          echo "critical=$CRITICAL" >> $GITHUB_OUTPUT
          echo "high=$HIGH" >> $GITHUB_OUTPUT
          echo "medium=$MEDIUM" >> $GITHUB_OUTPUT
          
          # 🔥 BARRAR APENAS: CRITICAL e HIGH (MEDIUM NÃO BLOQUEIA)
          if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 0 ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ Found $CRITICAL CRITICAL and $HIGH HIGH vulnerabilities!"
          else
            echo "status=passed" >> $GITHUB_OUTPUT
            echo "✅ No CRITICAL or HIGH vulnerabilities found"
          fi

  # ---------- SCAN 3: SYFT (SBOM) ----------
  syft-scan:
    needs: build-image
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check.outputs.status }}
      syft_json: syft-sbom.json
    steps:
      - name: 📥 Download image tar
        uses: actions/download-artifact@v8
        with:
          name: docker-image-tar
          path: /tmp

      - name: 📦 Load image from tar
        run: |
          echo "📦 Loading image from ${{ env.IMAGE_TAR }}"
          docker load -i ${{ env.IMAGE_TAR }}
          docker images

      - name: 📦 Install Syft
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
          syft version

      - name: 📋 Generate SBOM with Syft
        run: |
          syft ${{ env.IMAGE_FULL }} -o json > syft-sbom.json
          syft ${{ env.IMAGE_FULL }} -o table > syft-sbom.txt
          syft ${{ env.IMAGE_FULL }} -o cyclonedx-json > syft-sbom-cyclonedx.json

      - name: 📊 Generate Syft report
        run: |
          echo "## 📦 Syft SBOM Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f "syft-sbom.json" ]; then
            PACKAGES=$(grep -o '"name":' syft-sbom.json | wc -l || echo "0")
            echo "**Total packages discovered:** $PACKAGES" >> $GITHUB_STEP_SUMMARY
          fi

      - name: 📤 Upload Syft results
        uses: actions/upload-artifact@v7.0.1
        with:
          name: syft-sbom-results
          path: |
            syft-sbom.json
            syft-sbom.txt
            syft-sbom-cyclonedx.json
          retention-days: 7

      - name: ✅ Check scan status
        id: check
        run: |
          if [ -f "syft-sbom.json" ]; then
            echo "status=passed" >> $GITHUB_OUTPUT
            echo "✅ SBOM generated successfully"
          else
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ SBOM generation failed"
          fi

  # ---------- SCAN 4: DOCKLE ----------
  dockle-scan:
    needs: build-image
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check.outputs.status }}
    steps:
      - name: 📥 Download image tar
        uses: actions/download-artifact@v8
        with:
          name: docker-image-tar
          path: /tmp

      - name: 📦 Load image from tar
        run: |
          echo "📦 Loading image from ${{ env.IMAGE_TAR }}"
          docker load -i ${{ env.IMAGE_TAR }}
          docker images

      - name: 📦 Install Dockle
        run: |
          VERSION=$(curl --silent "https://api.github.com/repos/goodwithtech/dockle/releases/latest" | grep '"tag_name":' | sed -E 's/.*"v([^"]+)".*/\1/')
          curl -sSfL -o dockle.deb "https://github.com/goodwithtech/dockle/releases/download/v${VERSION}/dockle_${VERSION}_Linux-64bit.deb"
          sudo dpkg -i dockle.deb > /dev/null 2>&1
          sudo apt-get install -f -y > /dev/null 2>&1
          rm -f dockle.deb
          dockle --version

      - name: 🛡️ Scan with Dockle
        run: |
          dockle -f json ${{ env.IMAGE_FULL }} > dockle-results.json
          dockle ${{ env.IMAGE_FULL }} > dockle-results.txt

      - name: 📊 Generate Dockle report
        run: |
          echo "## 🛡️ Dockle Best Practices Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f "dockle-results.json" ]; then
            FATAL=$(grep -o '"level":"FATAL"' dockle-results.json | wc -l || echo "0")
            WARN=$(grep -o '"level":"WARN"' dockle-results.json | wc -l || echo "0")
            echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| 🔴 **Fatal** | $FATAL |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 **Warn** | $WARN |" >> $GITHUB_STEP_SUMMARY
          fi

      - name: 📤 Upload Dockle results
        uses: actions/upload-artifact@v7.0.1
        with:
          name: dockle-results
          path: |
            dockle-results.json
            dockle-results.txt
          retention-days: 7

      - name: ✅ Check scan status
        id: check
        run: |
          if [ ! -f "dockle-results.json" ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ Scan failed - no results file"
            exit 0
          fi
          FATAL=$(grep -o '"level":"FATAL"' dockle-results.json | wc -l || echo "0")
          if [ "$FATAL" -gt 0 ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ Found $FATAL FATAL issues!"
          else
            echo "status=passed" >> $GITHUB_OUTPUT
            echo "✅ No FATAL issues found"
          fi

  # ---------- SCAN 5: SNYK ----------
  snyk-scan:
    needs: build-image
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.check.outputs.status }}
      critical: ${{ steps.check.outputs.critical }}
      high: ${{ steps.check.outputs.high }}
      medium: ${{ steps.check.outputs.medium }}
    steps:
      - name: 📥 Download image tar
        uses: actions/download-artifact@v8
        with:
          name: docker-image-tar
          path: /tmp

      - name: 📦 Load image from tar
        run: |
          echo "📦 Loading image from ${{ env.IMAGE_TAR }}"
          docker load -i ${{ env.IMAGE_TAR }}
          docker images

      - name: 📦 Install Snyk
        run: |
          curl -sSfL https://static.snyk.io/cli/latest/snyk-linux -o snyk
          chmod +x snyk
          sudo mv snyk /usr/local/bin/
          snyk --version

      - name: 🚀 Scan with Snyk
        run: |
          snyk container test ${{ env.IMAGE_FULL }} --json > snyk-results.json || true
          snyk container test ${{ env.IMAGE_FULL }} --sarif > snyk-results.sarif || true

      - name: 📊 Generate Snyk report
        run: |
          echo "## 🚀 Snyk Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Severity | Count |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          if [ -f "snyk-results.json" ]; then
            CRITICAL=$(grep -o '"severity":"critical"' snyk-results.json | wc -l || echo "0")
            HIGH=$(grep -o '"severity":"high"' snyk-results.json | wc -l || echo "0")
            MEDIUM=$(grep -o '"severity":"medium"' snyk-results.json | wc -l || echo "0")
            echo "| 🔴 **Critical** | $CRITICAL |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟠 **High** | $HIGH |" >> $GITHUB_STEP_SUMMARY
            echo "| 🟡 **Medium** | $MEDIUM |" >> $GITHUB_STEP_SUMMARY

            # --- Lista resumida de vulnerabilidades (IDs) ---
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📌 Vulnerabilidades encontradas (amostra)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            for SEV in critical high medium; do
              COUNT=$(grep -o "\"severity\":\"$SEV\"" snyk-results.json | wc -l)
              if [ "$COUNT" -gt 0 ]; then
                # Pega os primeiros 10 IDs (campo "id" dentro de "identifiers" ou "CVE" diretamente)
                IDS=$(grep -B10 "\"severity\":\"$SEV\"" snyk-results.json | grep -o '"CVE-[0-9]*-[0-9]*"' | head -10 | paste -sd ", " -)
                if [ -z "$IDS" ]; then
                  # fallback: tenta outro campo
                  IDS=$(grep -B10 "\"severity\":\"$SEV\"" snyk-results.json | grep -o '"id":"[^"]*"' | sed 's/"id":"//;s/"//' | head -10 | paste -sd ", " -)
                fi
                echo "**${SEV^}** ($COUNT): $IDS" >> $GITHUB_STEP_SUMMARY
                if [ "$COUNT" -gt 10 ]; then
                  echo "  _(+ $((COUNT - 10)) outros)_" >> $GITHUB_STEP_SUMMARY
                fi
                echo "" >> $GITHUB_STEP_SUMMARY
              fi
            done
          fi

      - name: 📤 Upload Snyk results
        uses: actions/upload-artifact@v7.0.1
        with:
          name: snyk-results
          path: |
            snyk-results.json
            snyk-results.sarif
          retention-days: 7

      - name: ✅ Check scan status
        id: check
        run: |
          if [ ! -f "snyk-results.json" ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "critical=0" >> $GITHUB_OUTPUT
            echo "high=0" >> $GITHUB_OUTPUT
            echo "medium=0" >> $GITHUB_OUTPUT
            echo "❌ Scan failed - no results file"
            exit 0
          fi
          CRITICAL=$(grep -o '"severity":"critical"' snyk-results.json | wc -l || echo "0")
          HIGH=$(grep -o '"severity":"high"' snyk-results.json | wc -l || echo "0")
          MEDIUM=$(grep -o '"severity":"medium"' snyk-results.json | wc -l || echo "0")
          echo "critical=$CRITICAL" >> $GITHUB_OUTPUT
          echo "high=$HIGH" >> $GITHUB_OUTPUT
          echo "medium=$MEDIUM" >> $GITHUB_OUTPUT
          
          # 🔥 BARRAR APENAS: CRITICAL e HIGH (MEDIUM NÃO BLOQUEIA)
          if [ "$CRITICAL" -gt 0 ] || [ "$HIGH" -gt 0 ]; then
            echo "status=failed" >> $GITHUB_OUTPUT
            echo "❌ Found $CRITICAL CRITICAL and $HIGH HIGH vulnerabilities!"
          else
            echo "status=passed" >> $GITHUB_OUTPUT
            echo "✅ No CRITICAL or HIGH vulnerabilities found"
          fi

  # ================================================================
  # JOB 4: VALIDAÇÃO DOS SCANS
  # ================================================================
  validate-scans:
    needs: [trivy-scan, grype-scan, syft-scan, dockle-scan, snyk-scan]
    runs-on: ubuntu-latest
    if: always()
    outputs:
      scan_status: ${{ steps.validate.outputs.status }}
    steps:
      - name: 🔍 Validating all scan results
        id: validate
        run: |
          echo "## 🔍 Security Scans Validation" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tool | Status | Critical | High | Medium |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|----------|------|--------|" >> $GITHUB_STEP_SUMMARY
          
          FAILED=0
          
          # Trivy
          if [ "${{ needs.trivy-scan.outputs.status }}" == "passed" ]; then
            echo "| **Trivy** | ✅ Passed | ${{ needs.trivy-scan.outputs.critical }} | ${{ needs.trivy-scan.outputs.high }} | ${{ needs.trivy-scan.outputs.medium }} |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **Trivy** | ❌ Failed | ${{ needs.trivy-scan.outputs.critical }} | ${{ needs.trivy-scan.outputs.high }} | ${{ needs.trivy-scan.outputs.medium }} |" >> $GITHUB_STEP_SUMMARY
            FAILED=$((FAILED + 1))
          fi
          
          # Grype
          if [ "${{ needs.grype-scan.outputs.status }}" == "passed" ]; then
            echo "| **Grype** | ✅ Passed | ${{ needs.grype-scan.outputs.critical }} | ${{ needs.grype-scan.outputs.high }} | ${{ needs.grype-scan.outputs.medium }} |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **Grype** | ❌ Failed | ${{ needs.grype-scan.outputs.critical }} | ${{ needs.grype-scan.outputs.high }} | ${{ needs.grype-scan.outputs.medium }} |" >> $GITHUB_STEP_SUMMARY
            FAILED=$((FAILED + 1))
          fi
          
          # Syft
          if [ "${{ needs.syft-scan.outputs.status }}" == "passed" ]; then
            echo "| **Syft** | ✅ Passed | - | - | - |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **Syft** | ❌ Failed | - | - | - |" >> $GITHUB_STEP_SUMMARY
            FAILED=$((FAILED + 1))
          fi
          
          # Dockle
          if [ "${{ needs.dockle-scan.outputs.status }}" == "passed" ]; then
            echo "| **Dockle** | ✅ Passed | - | - | - |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **Dockle** | ❌ Failed | - | - | - |" >> $GITHUB_STEP_SUMMARY
            FAILED=$((FAILED + 1))
          fi
          
          # Snyk
          if [ "${{ needs.snyk-scan.outputs.status }}" == "passed" ]; then
            echo "| **Snyk** | ✅ Passed | ${{ needs.snyk-scan.outputs.critical }} | ${{ needs.snyk-scan.outputs.high }} | ${{ needs.snyk-scan.outputs.medium }} |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **Snyk** | ❌ Failed | ${{ needs.snyk-scan.outputs.critical }} | ${{ needs.snyk-scan.outputs.high }} | ${{ needs.snyk-scan.outputs.medium }} |" >> $GITHUB_STEP_SUMMARY
            FAILED=$((FAILED + 1))
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ $FAILED -eq 0 ]; then
            echo "✅ **All scans passed!** Ready to push." >> $GITHUB_STEP_SUMMARY
            echo "status=passed" >> $GITHUB_OUTPUT
          else
            echo "❌ **$FAILED scan(s) failed!** Please review the results." >> $GITHUB_STEP_SUMMARY
            echo "status=failed" >> $GITHUB_OUTPUT
          fi

  # ================================================================
  # JOB 5: CARTOGRAPHY + TRIVY (ANÁLISE DE VULNERABILIDADES)
  # ================================================================
  cartography-trivy-analysis:
    needs: [build-image, trivy-scan, validate-scans]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7.0.0

      - name: 📥 Download Trivy results
        uses: actions/download-artifact@v8
        with:
          name: trivy-scan-results
          path: /tmp/trivy
        continue-on-error: true

      - name: ⚠️ Check if Trivy results exist
        id: check-trivy
        run: |
          if [ -f "/tmp/trivy/trivy-results.sarif" ]; then
            echo "✅ Trivy results found"
            echo "trivy_exists=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Trivy results not found - skipping Trivy analysis"
            echo "trivy_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: 🐍 Setup Python
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        uses: actions/setup-python@v6.3.0
        with:
          python-version: '3.10'

      - name: 📦 Install Cartography and dependencies
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        run: |
          pip install cartography neo4j
          cartography --version

      - name: 🗄️ Start Neo4j in background
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        run: |
          docker run -d --name neo4j-trivy \
            -p 7474:7474 -p 7687:7687 \
            -e NEO4J_AUTH=neo4j/testpassword \
            neo4j:latest
          sleep 15

      - name: 🔄 Parse Trivy results and create graph data
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        run: |
          python3 << 'EOF'
          import json
          from datetime import datetime
          
          # Read SARIF file
          with open('/tmp/trivy/trivy-results.sarif', 'r') as f:
              sarif_data = json.load(f)
          
          # Structure for graph nodes and relationships
          graph_data = {
              'image': {
                  'name': '${{ env.IMAGE_FULL }}',
                  'tag': '${{ env.IMAGE_TAG }}',
                  'scanned_at': datetime.now().isoformat(),
                  'scanner': 'trivy'
              },
              'vulnerabilities': [],
              'packages': [],
              'relationships': []
          }
          
          packages_set = set()
          package_map = {}
          
          for run in sarif_data.get('runs', []):
              for result in run.get('results', []):
                  rule_id = result.get('ruleId', '')
                  severity = result.get('level', 'UNKNOWN').upper()
                  title = result.get('message', {}).get('text', '')
                  
                  locations = result.get('locations', [])
                  package = 'unknown'
                  version = 'unknown'
                  
                  if locations:
                      artifact = locations[0].get('physicalLocation', {}).get('artifactLocation', {})
                      if artifact:
                          uri = artifact.get('uri', '')
                          if '@' in uri:
                              parts = uri.split('@')
                              package = parts[0]
                              version = parts[1] if len(parts) > 1 else 'unknown'
                          elif '/' in uri:
                              path_parts = uri.split('/')
                              if path_parts:
                                  package = path_parts[-1]
                  
                  vuln_id = f"trivy_vuln_{len(graph_data['vulnerabilities'])}"
                  vulnerability = {
                      'id': vuln_id,
                      'cve': rule_id,
                      'severity': severity,
                      'title': title[:100] if title else 'No description',
                      'package': package,
                      'version': version,
                      'scanner': 'trivy',
                      'scanned_at': datetime.now().isoformat()
                  }
                  graph_data['vulnerabilities'].append(vulnerability)
                  
                  package_key = f"{package}:{version}"
                  if package_key not in packages_set and package != 'unknown':
                      packages_set.add(package_key)
                      pkg_id = f"trivy_pkg_{len(graph_data['packages'])}"
                      package_map[package_key] = pkg_id
                      graph_data['packages'].append({
                          'id': pkg_id,
                          'name': package,
                          'version': version,
                          'scanner': 'trivy'
                      })
                  
                  if package != 'unknown' and package_key in package_map:
                      graph_data['relationships'].append({
                          'type': 'AFFECTS',
                          'from': vuln_id,
                          'to': package_map[package_key]
                      })
          
          with open('trivy-graph-data.json', 'w') as f:
              json.dump(graph_data, f, indent=2)
          
          print(f'✅ Trivy: Parsed {len(graph_data["vulnerabilities"])} vulnerabilities')
          print(f'📦 Trivy: Found {len(graph_data["packages"])} unique packages')
          EOF

      - name: 📤 Import Trivy data to Neo4j
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        run: |
          python3 << 'EOF'
          from neo4j import GraphDatabase
          import json
          
          driver = GraphDatabase.driver(
              'bolt://localhost:7687',
              auth=('neo4j', 'testpassword')
          )
          
          with open('trivy-graph-data.json', 'r') as f:
              data = json.load(f)
          
          with driver.session() as session:
              # Image node
              session.run(
                  """
                  MERGE (img:Image {
                      name: $name,
                      tag: $tag,
                      scanned_at: $scanned_at
                  })
                  """,
                  **data['image']
              )
              print('✅ Trivy: Image node created')
              
              # Package nodes
              for pkg in data['packages']:
                  session.run(
                      """
                      MERGE (p:TrivyPackage {
                          id: $id,
                          name: $name,
                          version: $version,
                          scanner: $scanner
                      })
                      """,
                      **pkg
                  )
              print(f'✅ Trivy: {len(data["packages"])} Package nodes created')
              
              # Vulnerability nodes
              for vuln in data['vulnerabilities']:
                  session.run(
                      """
                      MERGE (v:TrivyFinding {
                          id: $id,
                          cve: $cve,
                          severity: $severity,
                          title: $title,
                          package: $package,
                          version: $version,
                          scanner: $scanner,
                          scanned_at: $scanned_at
                      })
                      """,
                      **vuln
                  )
              print(f'✅ Trivy: {len(data["vulnerabilities"])} Vulnerability nodes created')
              
              # Relationships
              for rel in data['relationships']:
                  if rel['to'] is not None:
                      session.run(
                          """
                          MATCH (v:TrivyFinding {id: $from})
                          MATCH (p:TrivyPackage {id: $to})
                          MERGE (v)-[:AFFECTS]->(p)
                          """,
                          **rel
                      )
              print('✅ Trivy: Relationships created')
              
              # Link to Image
              session.run(
                  """
                  MATCH (v:TrivyFinding)
                  MATCH (img:Image {name: $image_name})
                  MERGE (v)-[:AFFECTS_IMAGE]->(img)
                  """,
                  image_name=data['image']['name']
              )
              print('✅ Trivy: Vulnerabilities linked to Image')
              
          driver.close()
          print('✅ Trivy: All data imported successfully!')
          EOF

      - name: 📊 Generate Trivy Analysis Report
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        run: |
          python3 << 'EOF'
          from neo4j import GraphDatabase
          import json
          
          driver = GraphDatabase.driver(
              'bolt://localhost:7687',
              auth=('neo4j', 'testpassword')
          )
          
          with driver.session() as session:
              result = session.run(
                  """
                  MATCH (v:TrivyFinding)
                  RETURN v.severity as severity, count(v) as total
                  ORDER BY total DESC
                  """
              )
              severity_counts = {record['severity']: record['total'] for record in result}
              
              result = session.run(
                  """
                  MATCH (v:TrivyFinding)-[:AFFECTS]->(p:TrivyPackage)
                  RETURN p.name as package, count(v) as vuln_count
                  ORDER BY vuln_count DESC
                  LIMIT 5
                  """
              )
              top_packages = [{'package': record['package'], 'count': record['vuln_count']} for record in result]
              
              result = session.run(
                  """
                  MATCH (v:TrivyFinding {severity: 'CRITICAL'})
                  RETURN v.cve as cve, v.title as title, v.package as package
                  LIMIT 10
                  """
              )
              critical_vulns = [{'cve': record['cve'], 'title': record['title'], 'package': record['package']} for record in result]
              
              result = session.run(
                  """
                  MATCH (v:TrivyFinding {severity: 'HIGH'})
                  RETURN v.cve as cve, v.title as title, v.package as package
                  LIMIT 10
                  """
              )
              high_vulns = [{'cve': record['cve'], 'title': record['title'], 'package': record['package']} for record in result]
          
          driver.close()
          
          analysis = {
              'severity_counts': severity_counts,
              'top_affected_packages': top_packages,
              'critical_vulnerabilities': critical_vulns,
              'high_vulnerabilities': high_vulns
          }
          with open('trivy-analysis.json', 'w') as f:
              json.dump(analysis, f, indent=2)
          EOF

      - name: 📤 Upload Trivy analysis data
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: trivy-analysis
          path: |
            trivy-graph-data.json
            trivy-analysis.json
          retention-days: 7

      - name: 🧹 Cleanup Neo4j container
        if: steps.check-trivy.outputs.trivy_exists == 'true'
        run: |
          docker stop neo4j-trivy || true
          docker rm neo4j-trivy || true

  # ================================================================
  # JOB 6: CARTOGRAPHY + SYFT (ANÁLISE DE DEPENDÊNCIAS)
  # ================================================================
  cartography-syft-analysis:
    needs: [build-image, syft-scan, validate-scans]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7.0.0

      - name: 📥 Download Syft results
        uses: actions/download-artifact@v8
        with:
          name: syft-sbom-results
          path: /tmp/syft
        continue-on-error: true

      - name: ⚠️ Check if Syft results exist
        id: check-syft
        run: |
          if [ -f "/tmp/syft/syft-sbom.json" ]; then
            echo "✅ Syft results found"
            echo "syft_exists=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Syft results not found - skipping Syft analysis"
            echo "syft_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: 🐍 Setup Python
        if: steps.check-syft.outputs.syft_exists == 'true'
        uses: actions/setup-python@v6.3.0
        with:
          python-version: '3.10'

      - name: 📦 Install Cartography and dependencies
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          pip install cartography neo4j
          cartography --version

      - name: 🗄️ Start Neo4j in background
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          docker run -d --name neo4j-syft \
            -p 7475:7474 -p 7688:7687 \
            -e NEO4J_AUTH=neo4j/testpassword \
            neo4j:latest
          sleep 15

      - name: 🔄 Parse Syft results and create graph data
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          python3 << 'EOF'
          import json
          from datetime import datetime
          
          # Read Syft JSON
          with open('/tmp/syft/syft-sbom.json', 'r') as f:
              syft_data = json.load(f)
          
          graph_data = {
              'image': {
                  'name': '${{ env.IMAGE_FULL }}',
                  'tag': '${{ env.IMAGE_TAG }}',
                  'scanned_at': datetime.now().isoformat(),
                  'scanner': 'syft'
              },
              'packages': [],
              'dependencies': []
          }
          
          # Map artifact IDs to package IDs
          artifact_map = {}
          
          # Process artifacts (packages)
          for artifact in syft_data.get('artifacts', []):
              pkg_id = artifact.get('id', '')
              pkg_name = artifact.get('name', 'unknown')
              pkg_version = artifact.get('version', 'unknown')
              pkg_type = artifact.get('type', 'unknown')
              
              artifact_map[pkg_id] = {
                  'id': f"syft_pkg_{len(graph_data['packages'])}",
                  'name': pkg_name,
                  'version': pkg_version,
                  'type': pkg_type
              }
              
              graph_data['packages'].append(artifact_map[pkg_id])
          
          # Process relationships (dependencies)
          for rel in syft_data.get('artifactRelationships', []):
              child_id = rel.get('child', '')
              parent_id = rel.get('parent', '')
              rel_type = rel.get('type', '')
              
              if child_id in artifact_map and parent_id in artifact_map:
                  graph_data['dependencies'].append({
                      'from': artifact_map[parent_id]['id'],
                      'to': artifact_map[child_id]['id'],
                      'type': rel_type.upper()
                  })
          
          with open('syft-graph-data.json', 'w') as f:
              json.dump(graph_data, f, indent=2)
          
          print(f'✅ Syft: Found {len(graph_data["packages"])} packages')
          print(f'🔗 Syft: Found {len(graph_data["dependencies"])} dependencies')
          EOF

      - name: 📤 Import Syft data to Neo4j
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          python3 << 'EOF'
          from neo4j import GraphDatabase
          import json
          
          driver = GraphDatabase.driver(
              'bolt://localhost:7688',
              auth=('neo4j', 'testpassword')
          )
          
          with open('syft-graph-data.json', 'r') as f:
              data = json.load(f)
          
          with driver.session() as session:
              # Image node
              session.run(
                  """
                  MERGE (img:SyftImage {
                      name: $name,
                      tag: $tag,
                      scanned_at: $scanned_at,
                      scanner: $scanner
                  })
                  """,
                  **data['image']
              )
              print('✅ Syft: Image node created')
              
              # Package nodes (SyftPackage)
              for pkg in data['packages']:
                  session.run(
                      """
                      MERGE (p:SyftPackage {
                          id: $id,
                          name: $name,
                          version: $version,
                          type: $type
                      })
                      """,
                      **pkg
                  )
              print(f'✅ Syft: {len(data["packages"])} Package nodes created')
              
              # Dependency relationships (DEPENDS_ON)
              for dep in data['dependencies']:
                  session.run(
                      """
                      MATCH (from:SyftPackage {id: $from})
                      MATCH (to:SyftPackage {id: $to})
                      MERGE (from)-[:DEPENDS_ON {type: $type}]->(to)
                      """,
                      **dep
                  )
              print(f'✅ Syft: {len(data["dependencies"])} Dependency relationships created')
              
              # Link packages to Image
              session.run(
                  """
                  MATCH (p:SyftPackage)
                  MATCH (img:SyftImage {name: $image_name})
                  MERGE (p)-[:PART_OF_IMAGE]->(img)
                  """,
                  image_name=data['image']['name']
              )
              print('✅ Syft: Packages linked to Image')
              
          driver.close()
          print('✅ Syft: All data imported successfully!')
          EOF

      - name: 📊 Generate Syft Analysis Report
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          python3 << 'EOF'
          from neo4j import GraphDatabase
          import json
          
          driver = GraphDatabase.driver(
              'bolt://localhost:7688',
              auth=('neo4j', 'testpassword')
          )
          
          with driver.session() as session:
              # Total packages
              result = session.run("MATCH (p:SyftPackage) RETURN count(p) as total")
              total_packages = result.single()['total']
              
              # Packages by type
              result = session.run(
                  """
                  MATCH (p:SyftPackage)
                  RETURN p.type as type, count(p) as total
                  ORDER BY total DESC
                  """
              )
              packages_by_type = {record['type']: record['total'] for record in result}
              
              # Top 10 dependency chains
              result = session.run(
                  """
                  MATCH path = (p:SyftPackage)-[:DEPENDS_ON*1..3]->(dep:SyftPackage)
                  WHERE NOT exists((p)<-[:DEPENDS_ON]-())
                  RETURN p.name as root, dep.name as dependency, length(path) as depth
                  LIMIT 10
                  """
              )
              dependency_chains = [
                  {'root': record['root'], 'dependency': record['dependency'], 'depth': record['depth']}
                  for record in result
              ]
              
              # Most depended on packages
              result = session.run(
                  """
                  MATCH (p:SyftPackage)<-[:DEPENDS_ON]-(dep:SyftPackage)
                  RETURN p.name as package, count(dep) as dependents
                  ORDER BY dependents DESC
                  LIMIT 5
                  """
              )
              most_depended = [
                  {'package': record['package'], 'dependents': record['dependents']}
                  for record in result
              ]
          
          driver.close()
          
          analysis = {
              'total_packages': total_packages,
              'packages_by_type': packages_by_type,
              'dependency_chains': dependency_chains,
              'most_depended_on': most_depended
          }
          with open('syft-analysis.json', 'w') as f:
              json.dump(analysis, f, indent=2)
          EOF

      - name: 📊 Generate Syft Report
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          echo "## 🗺️ Cartography + Syft Dependency Analysis" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          python3 << 'EOF' >> $GITHUB_STEP_SUMMARY
          import json
          
          with open('syft-analysis.json', 'r') as f:
              data = json.load(f)
          
          print(f"| **Image** | ${{ env.IMAGE_FULL }} |")
          print(f"| **Tag** | ${{ env.IMAGE_TAG }} |")
          print(f"| **Total Packages** | {data['total_packages']} |")
          print("")
          
          print("### 📦 Packages by Type")
          print("")
          print("| Type | Count |")
          print("|------|-------|")
          for pkg_type, count in data['packages_by_type'].items():
              print(f"| {pkg_type} | {count} |")
          
          print("")
          print("### 🔗 Most Depended On Packages")
          print("")
          print("| Package | Dependents |")
          print("|---------|------------|")
          for pkg in data['most_depended_on']:
              print(f"| {pkg['package']} | {pkg['dependents']} |")
          
          print("")
          print("### 🌳 Sample Dependency Chains")
          print("")
          print("| Root Package | Dependency | Depth |")
          print("|--------------|------------|-------|")
          for chain in data['dependency_chains'][:5]:
              print(f"| {chain['root']} | {chain['dependency']} | {chain['depth']} |")
          EOF

      - name: 🔍 Sample Cypher Queries for Syft
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔍 Useful Cypher Queries for Syft (Neo4j on port 7688)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```cypher' >> $GITHUB_STEP_SUMMARY
          echo '# Browse dependency tree' >> $GITHUB_STEP_SUMMARY
          echo 'MATCH path = (p:SyftPackage)-[:DEPENDS_ON*1..5]->(dep:SyftPackage)' >> $GITHUB_STEP_SUMMARY
          echo 'WHERE NOT exists((p)<-[:DEPENDS_ON]-())' >> $GITHUB_STEP_SUMMARY
          echo 'RETURN path LIMIT 25' >> $GITHUB_STEP_SUMMARY
          echo '' >> $GITHUB_STEP_SUMMARY
          echo '# Find packages that depend on a specific package' >> $GITHUB_STEP_SUMMARY
          echo 'MATCH (upstream:SyftPackage)-[:DEPENDS_ON*1..10]->(dep:SyftPackage {name: "openssl"})' >> $GITHUB_STEP_SUMMARY
          echo 'RETURN DISTINCT upstream.name' >> $GITHUB_STEP_SUMMARY
          echo '' >> $GITHUB_STEP_SUMMARY
          echo '# Find packages with most dependents' >> $GITHUB_STEP_SUMMARY
          echo 'MATCH (p:SyftPackage)<-[:DEPENDS_ON]-(dep:SyftPackage)' >> $GITHUB_STEP_SUMMARY
          echo 'RETURN p.name, count(dep) AS dependents ORDER BY dependents DESC LIMIT 10' >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: 📤 Upload Syft analysis data
        if: steps.check-syft.outputs.syft_exists == 'true'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: syft-analysis
          path: |
            syft-graph-data.json
            syft-analysis.json
          retention-days: 7

      - name: 🧹 Cleanup Neo4j container
        if: steps.check-syft.outputs.syft_exists == 'true'
        run: |
          docker stop neo4j-syft || true
          docker rm neo4j-syft || true

  # ================================================================
  # JOB 7: DIAGRAMAS (EXCALIDRAW & MERMAID) - GERAÇÃO E COLETA
  # ================================================================
  generate-diagrams:
    needs: [checkout-code]
    runs-on: ubuntu-latest
    if: always()
    outputs:
      diagrams_generated: ${{ steps.check-diagrams.outputs.generated }}
    steps:
      - name: 🔍 Checkout code
        uses: actions/checkout@v7.0.0

      - name: 🔎 Procurar fontes e imagens de diagramas
        id: check-diagrams
        run: |
          # Define diretórios comuns para diagramas
          DIAGRAM_DIRS="docs/diagrams diagrams docs ."
          FOUND_SOURCE=false
          FOUND_IMAGES=false
          
          # Procura por arquivos fonte
          for dir in $DIAGRAM_DIRS; do
            if [ -d "$dir" ]; then
              if find "$dir" -maxdepth 3 \( -name "*.mmd" -o -name "*.excalidraw" \) | grep -q .; then
                echo "✅ Found diagram source files in $dir"
                FOUND_SOURCE=true
                break
              fi
            fi
          done
          
          # Procura por imagens já renderizadas (PNG, SVG)
          for dir in $DIAGRAM_DIRS; do
            if [ -d "$dir" ]; then
              if find "$dir" -maxdepth 3 \( -name "*.png" -o -name "*.svg" \) | grep -q .; then
                echo "✅ Found existing diagram images in $dir"
                FOUND_IMAGES=true
                break
              fi
            fi
          done
          
          if [ "$FOUND_SOURCE" = true ] || [ "$FOUND_IMAGES" = true ]; then
            echo "generated=true" >> $GITHUB_OUTPUT
            echo "✅ Diagram assets found (source or images)."
          else
            echo "generated=false" >> $GITHUB_OUTPUT
            echo "⚠️ No diagram sources or images found – skipping diagram processing."
          fi

      - name: 📦 Install Node.js (para renderização)
        if: steps.check-diagrams.outputs.generated == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 🖼️ Install Mermaid CLI e Excalidraw CLI (se houver fontes)
        if: steps.check-diagrams.outputs.generated == 'true'
        run: |
          # Verifica se há arquivos fonte antes de instalar as ferramentas
          if find . -type f \( -name "*.mmd" -o -name "*.excalidraw" \) | grep -q .; then
            npm install -g @mermaid-js/mermaid-cli excalidraw-cli
          else
            echo "No source files found, skipping installation."
          fi

      - name: 🎨 Renderizar diagramas (se houver fontes)
        if: steps.check-diagrams.outputs.generated == 'true'
        run: |
          # Renderiza Mermaid
          find . -type f -name "*.mmd" | while read -r file; do
            dir=$(dirname "$file")
            base=$(basename "$file" .mmd)
            echo "🔄 Rendering Mermaid: $file -> $dir/${base}.png"
            mmdc -i "$file" -o "$dir/${base}.png" || echo "⚠️ Failed to render $file"
          done
          
          # Renderiza Excalidraw
          find . -type f -name "*.excalidraw" | while read -r file; do
            dir=$(dirname "$file")
            base=$(basename "$file" .excalidraw)
            echo "🔄 Rendering Excalidraw: $file -> $dir/${base}.png"
            excalidraw-cli render "$file" --output "$dir/${base}.png" || echo "⚠️ Failed to render $file"
          done

      - name: 📤 Coletar e enviar imagens de diagramas
        if: steps.check-diagrams.outputs.generated == 'true'
        run: |
          # Cria um diretório para coletar as imagens
          mkdir -p diagram_images
          
          # Copia todas as imagens .png e .svg encontradas nos diretórios comuns
          DIAGRAM_DIRS="docs/diagrams diagrams docs ."
          for dir in $DIAGRAM_DIRS; do
            if [ -d "$dir" ]; then
              find "$dir" -maxdepth 3 \( -name "*.png" -o -name "*.svg" \) -exec cp {} diagram_images/ \; 2>/dev/null || true
            fi
          done
          
          # Remove duplicatas (mantém a primeira encontrada)
          ls diagram_images/* 2>/dev/null | sort -u | while read -r file; do
            base=$(basename "$file")
            # Se já existe um arquivo com o mesmo nome, mantém o primeiro
            if [ -f "diagram_images/$base" ] && [ "diagram_images/$base" != "$file" ]; then
              rm -f "$file"
            fi
          done
          
          echo "📊 Total de imagens coletadas: $(ls diagram_images/ 2>/dev/null | wc -l)"

      - name: 📤 Upload imagens de diagramas (se houver)
        if: steps.check-diagrams.outputs.generated == 'true'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: diagram-images
          path: diagram_images/
          retention-days: 7

  # ================================================================
  # JOB 8: EMPACOTAR DIAGRAMAS E EXIBIR NO SUMMARY
  # ================================================================
  package-diagrams:
    needs: [generate-diagrams]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 📥 Baixar artefato de imagens (se existir)
        uses: actions/download-artifact@v8
        continue-on-error: true
        with:
          name: diagram-images
          path: ./diagram_images

      - name: 🔍 Verificar se há imagens
        id: check-images
        run: |
          # Se o artefato não foi baixado, tenta buscar no workspace (fallback)
          if [ ! -d "./diagram_images" ] || [ -z "$(ls -A ./diagram_images 2>/dev/null)" ]; then
            echo "⚠️ Nenhuma imagem baixada. Tentando buscar localmente..."
            mkdir -p diagram_images
            DIAGRAM_DIRS="docs/diagrams diagrams docs ."
            for dir in $DIAGRAM_DIRS; do
              if [ -d "$dir" ]; then
                find "$dir" -maxdepth 3 \( -name "*.png" -o -name "*.svg" \) -exec cp {} diagram_images/ \; 2>/dev/null || true
              fi
            done
          fi
          
          if [ -d "./diagram_images" ] && [ "$(ls -A ./diagram_images 2>/dev/null)" ]; then
            echo "✅ Encontradas $(ls ./diagram_images | wc -l) imagens."
            echo "has_images=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Nenhuma imagem de diagrama encontrada."
            echo "has_images=false" >> $GITHUB_OUTPUT
          fi

      - name: 📦 Compactar imagens em diagrams.zip
        if: steps.check-images.outputs.has_images == 'true'
        run: |
          cd diagram_images
          zip -r ../diagrams.zip *
          cd ..
          echo "ZIP_SIZE=$(du -sh diagrams.zip | cut -f1)" >> $GITHUB_ENV

      - name: 📤 Upload diagrams.zip como artefato
        if: steps.check-images.outputs.has_images == 'true'
        uses: actions/upload-artifact@v7.0.1
        with:
          name: diagrams
          path: diagrams.zip
          retention-days: 7

      - name: 📝 Adicionar seção de diagramas ao Summary
        if: steps.check-images.outputs.has_images == 'true'
        run: |
          echo "## 🖼️ Diagramas (Excalidraw & Mermaid)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📦 **Pacote completo:** [diagrams.zip](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Miniaturas (imagens ≤ 1 MB)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Itera sobre as imagens, codifica e exibe as que têm até 1 MB
          find ./diagram_images -type f \( -name "*.png" -o -name "*.svg" \) | while read -r img; do
            # Verifica o tamanho do arquivo (em bytes)
            size=$(stat -c%s "$img" 2>/dev/null || stat -f%z "$img" 2>/dev/null)
            if [ "$size" -le 1048576 ]; then  # 1 MB
              filename=$(basename "$img")
              echo "**$filename**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              # Codifica e insere como imagem inline (largura máxima 400px)
              base64 -w0 "$img" | { echo -n '<img src="data:image/png;base64,'; cat; echo '" width="400" />'; } >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️ Imagem muito grande para miniatura: $(basename "$img") ($(du -h "$img" | cut -f1)) – disponível apenas no .zip" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi
          done

      - name: ⚠️ Aviso se nenhum diagrama foi encontrado
        if: steps.check-images.outputs.has_images == 'false'
        run: |
          echo "## 🖼️ Diagramas" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "⚠️ Nenhum diagrama (Excalidraw ou Mermaid) foi encontrado." >> $GITHUB_STEP_SUMMARY

  # ================================================================
  # JOB 9: PUSH DA IMAGEM PARA O REGISTRY (SÓ SE APROVADO)
  # ================================================================
  push-image:
    needs: [build-image, validate-scans, cartography-trivy-analysis, cartography-syft-analysis]
    runs-on: ubuntu-latest
    if: needs.validate-scans.outputs.scan_status == 'passed'
    steps:
      - name: 📥 Download image tar
        uses: actions/download-artifact@v8
        with:
          name: docker-image-tar
          path: /tmp

      - name: 📦 Load image from tar
        run: |
          echo "📦 Loading image from ${{ env.IMAGE_TAR }}"
          docker load -i ${{ env.IMAGE_TAR }}
          echo "📦 Images loaded:"
          docker images --format "{{.Repository}}:{{.Tag}}" | grep "${{ env.IMAGE_NAME }}"

      - name: 🔑 Login to Docker Hub
        uses: docker/login-action@v4.4.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🚀 Push Docker image to Registry
        run: |
          echo "📦 Pushing image: ${{ env.IMAGE_FULL }}"
          echo "🔐 Image approved by security scans!"
          docker push ${{ env.IMAGE_FULL }}
          
          echo "📦 Pushing image: ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest"
          docker push ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest
          
          echo "✅ Images pushed successfully!"

      - name: 📊 Generate push report
        run: |
          echo "## 🚀 Image Push Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Information | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|-------------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Image Name** | ${{ env.IMAGE_NAME }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Tag** | ${{ env.IMAGE_TAG }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Full Image** | ${{ env.IMAGE_FULL }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Latest Tag** | ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE_NAME }}:latest |" >> $GITHUB_STEP_SUMMARY
          echo "| **Registry** | Docker Hub |" >> $GITHUB_STEP_SUMMARY
          echo "| **Security Status** | ✅ All scans passed |" >> $GITHUB_STEP_SUMMARY
          echo "| **Push Status** | ✅ Success |" >> $GITHUB_STEP_SUMMARY

  # ================================================================
  # JOB 10: RELATÓRIO FINAL COMPLETO (INCLUI DIAGRAMAS)
  # ================================================================
  final-report:
    needs: [checkout-code, build-image, trivy-scan, grype-scan, syft-scan, dockle-scan, snyk-scan, validate-scans, cartography-trivy-analysis, cartography-syft-analysis, push-image, package-diagrams]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: 📋 Generate Complete Pipeline Summary
        run: |
          echo "# 🎯 DevSecOps Pipeline Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## 📋 Pipeline Execution Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          
          # Checkout
          if [ "${{ needs.checkout-code.result }}" == "success" ]; then
            echo "| **1. Checkout Code** | ✅ Success |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **1. Checkout Code** | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Build
          if [ "${{ needs.build-image.result }}" == "success" ]; then
            echo "| **2. Build Image** | ✅ Success |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **2. Build Image** | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Scans
          echo "| **3a. Trivy Scan** | ${{ needs.trivy-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **3b. Grype Scan** | ${{ needs.grype-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **3c. Syft SBOM** | ${{ needs.syft-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **3d. Dockle Scan** | ${{ needs.dockle-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **3e. Snyk Scan** | ${{ needs.snyk-scan.result }} |" >> $GITHUB_STEP_SUMMARY
          
          # Validation
          if [ "${{ needs.validate-scans.outputs.scan_status }}" == "passed" ]; then
            echo "| **4. Security Validation** | ✅ All Passed |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **4. Security Validation** | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Cartography - Trivy
          if [ "${{ needs.cartography-trivy-analysis.result }}" == "success" ]; then
            echo "| **5a. Cartography + Trivy** | ✅ Success |" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.cartography-trivy-analysis.result }}" == "skipped" ]; then
            echo "| **5a. Cartography + Trivy** | ⏭️ Skipped (No results) |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **5a. Cartography + Trivy** | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Cartography - Syft
          if [ "${{ needs.cartography-syft-analysis.result }}" == "success" ]; then
            echo "| **5b. Cartography + Syft** | ✅ Success |" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.cartography-syft-analysis.result }}" == "skipped" ]; then
            echo "| **5b. Cartography + Syft** | ⏭️ Skipped (No results) |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **5b. Cartography + Syft** | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Push
          if [ "${{ needs.push-image.result }}" == "success" ]; then
            echo "| **6. Push to Registry** | ✅ Success |" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.push-image.result }}" == "skipped" ]; then
            echo "| **6. Push to Registry** | ⏭️ Skipped (Security failed) |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **6. Push to Registry** | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Diagramas
          if [ "${{ needs.package-diagrams.result }}" == "success" ]; then
            echo "| **7. Diagramas (Excalidraw/Mermaid)** | ✅ Processado |" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ needs.package-diagrams.result }}" == "skipped" ]; then
            echo "| **7. Diagramas (Excalidraw/Mermaid)** | ⏭️ Sem arquivos |" >> $GITHUB_STEP_SUMMARY
          else
            echo "| **7. Diagramas (Excalidraw/Mermaid)** | ❌ Falha |" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 Image Information" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| **Image** | ${{ env.IMAGE_FULL }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Tag** | ${{ env.IMAGE_TAG }} |" >> $GITHUB_STEP_SUMMARY
          
          # Vulnerability summary (já inclui listas nos jobs individuais)
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🔒 Vulnerability Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Tool | Critical | High | Medium | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|----------|------|--------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Trivy** | ${{ needs.trivy-scan.outputs.critical }} | ${{ needs.trivy-scan.outputs.high }} | ${{ needs.trivy-scan.outputs.medium }} | ${{ needs.trivy-scan.outputs.status }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Grype** | ${{ needs.grype-scan.outputs.critical }} | ${{ needs.grype-scan.outputs.high }} | ${{ needs.grype-scan.outputs.medium }} | ${{ needs.grype-scan.outputs.status }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Snyk** | ${{ needs.snyk-scan.outputs.critical }} | ${{ needs.snyk-scan.outputs.high }} | ${{ needs.snyk-scan.outputs.medium }} | ${{ needs.snyk-scan.outputs.status }} |" >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 SBOM Summary (Syft)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ needs.syft-scan.outputs.status }}" == "passed" ]; then
            echo "✅ **SBOM generated successfully - see Cartography + Syft analysis above**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **SBOM generation failed**" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ needs.validate-scans.outputs.scan_status }}" == "passed" ]; then
            echo "✅ **Image approved and pushed to registry successfully!**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Image NOT pushed due to security scan failures.**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 **Check the Cartography analyses above for detailed information:**" >> $GITHUB_STEP_SUMMARY
            echo "- **Trivy Analysis**: Vulnerability distribution and affected packages" >> $GITHUB_STEP_SUMMARY
            echo "- **Syft Analysis**: Dependency tree and package relationships" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "📌 **Os diagramas (Excalidraw/Mermaid) foram processados e estão disponíveis na seção acima.**" >> $GITHUB_STEP_SUMMARY
